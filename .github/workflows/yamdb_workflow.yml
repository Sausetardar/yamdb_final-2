name: Yamdb workoflow

on: [push]

jobs:

  tests:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 
      uses: actions/setup-python@v2
      with:
        python-version: 3.8

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 pytest
        pip install -r requirements.txt
    - name: Lint with flake8
      run: flake8 . --ignore E501,W503 --exclude *tests/,*migrations/

  build_and_push_to_docker_hub:
    name: Push Docker image to Docker Hub
    runs-on: ubuntu-latest
    needs: tests
    steps:
      - name: Check out the repo
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to Docker
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Push to Docker Hub
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: sorochinskii/yamdb_final:latest 

  deploy:
     runs-on: ubuntu-latest
     needs: build_and_push_to_docker_hub
     steps:
     - name: executing remote ssh commands to deploy
       uses: appleboy/ssh-action@master
       with:
         host: ${{ secrets.HOST }}
         username: ${{ secrets.USER }}
         key: ${{ secrets.SSH_KEY }}
         port: ${{ secrets.PORT }} 
         passphrase: ${{ secrets.SSH_PASSPHRASE }}
         script: |
           echo ${{ secrets.DEV_USER_PASSWORD }} | sudo -S docker pull sorochinskii/yamdb_final_web
           echo ${{ secrets.DEV_USER_PASSWORD }} | sudo -S docker stop $(echo ${{ secrets.DEV_USER_PASSWORD }} | sudo -S docker ps -a -q)
<<<<<<< HEAD
           echo ${{ secrets.DEV_USER_PASSWORD }} | sudo -S docker run --rm -d -p 5000:5000 sorochinskii/yamdb_final
  
=======
           echo ${{ secrets.DEV_USER_PASSWORD }} | sudo -S docker run --rm -d -p 5000:5000 sorochinskii/yamdb_final_web

>>>>>>> 00130b7751d9b7e967c19501780141e8f9791660
  send_message:
    runs-on: ubuntu-latest
    needs: deploy
    steps:
    - name: send message
      uses: appleboy/telegram-action@master
      with:
        to: ${{ secrets.TELEGRAM_TO }}
        token: ${{ secrets.TELEGRAM_TOKEN }}
        message: ${{ github.workflow }} успешно выполнен! 
